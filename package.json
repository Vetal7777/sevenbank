{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "concurrently 'nuxt dev' 'json-server --watch db.json'",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "json-server": "json-server --watch db.json"
  },
  "devDependencies": {
    "concurrently": "^7.6.0",
    "json-server": "^0.17.1",
    "nuxt": "^3.2.2"
  },
  "dependencies": {
    "@pinia/nuxt": "^0.4.7",
    "pinia": "^2.0.32",
    "sass": "^1.58.3",
    "sass-loader": "^11.0.1"
  }
}